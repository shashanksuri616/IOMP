CORS_ALLOW_ORIGINS=*
# Comma-separated list. Example for prod:
# CORS_ALLOW_ORIGINS=https://iomp.example.com,https://iomp-frontend.vercel.app

# Model choices (ensure installed in image or downloaded on first run)
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Logging verbosity
LOG_LEVEL=info
